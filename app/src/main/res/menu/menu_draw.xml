<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <!--   always：总是显示在界面上
       never：不显示在界面上，只让出现在右边的三个点中
       ifRoom：如果有位置才显示，不然就出现在右边的三个点中-->
    <item
        android:id="@+id/drawer_menu_setting"
        android:icon="@drawable/ic_menu_settings"
        android:title="@string/drawer_menu_setting"
         />

    <item android:id="@+id/drawer_menu_backup"
        android:icon="@drawable/ic_menu_backup"
        android:title="@string/drawer_menu_copy"/>
    <item android:id="@+id/drawer_menu_share"
        android:icon="@drawable/ic_menu_share"
        android:title="@string/drawer_menu_share"/>
    <item android:id="@+id/drawer_menu_rate"
        android:icon="@drawable/ic_menu_rate"
        android:title="@string/drawer_menu_rate"/>
</menu>