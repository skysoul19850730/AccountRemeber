<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jscoolstar.accountremeber">

    <application
        android:name=".apps.MApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ActivityThemeBase">
        <activity
            android:name=".activities.login.LoginActivity"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>
        <activity android:name=".activities.register.RegisterActivity"/>
        <activity android:name=".activities.settings.activities.MineAct"
            />
        <activity
            android:name=".activities.home.activities.MainActivity"
            android:launchMode="singleTask"
           ></activity>
        <activity
            android:name=".activities.register.activities.RegisterActivity"
            ></activity>
        <activity android:name=".activities.register.activities.Register2Activity"
            ></activity>
        <activity
            android:name=".activities.edit.activities.EditActivity"

            android:windowSoftInputMode="stateHidden|stateUnchanged" />
    </application>

</manifest>